const forwardButton = document.getElementById("forwardButton"); const backwardButton = document.getElementById("backwardButton"); const leftButton = document.getElementById("leftButton"); const stopButton = document.getElementById("stopButton"); const rightButton = document.getElementById("rightButton"); const slider = document.getElementById("slider"); const speed = document.getElementById("speed"); const MOVEMENTS = { STOP: 0, FORWARD: 1, BACKWARD: 2, RIGHT: 3, LEFT: 4 }; function sendRequest(value, path = "move", param = "direction") { var xhr = new XMLHttpRequest(); xhr.open("GET", `/${path}?${param}=${value}`, true); xhr.send() } forwardButton.addEventListener("click", () => { console.log("Forward"); sendRequest(MOVEMENTS.FORWARD) }); backwardButton.addEventListener("click", () => { console.log("Backward"); sendRequest(MOVEMENTS.BACKWARD) }); leftButton.addEventListener("click", () => { console.log("Left"); sendRequest(MOVEMENTS.LEFT) }); stopButton.addEventListener("click", () => { console.log("Stop"); sendRequest(MOVEMENTS.STOP) }); rightButton.addEventListener("click", () => { console.log("Right"); sendRequest(MOVEMENTS.RIGHT) }); function updateSliderPWM(element) { var value = slider.value; sendRequest(value, "speed", "value"); speed.innerText = value; console.log("Slider Value: " + value) }